syntax on

" Wrap gitcommit file types at the appropriate length
filetype indent plugin on

" Secure when using `vim -x filename` for encryption
set cm=blowfish2
set viminfo=
set nobackup
set nowritebackup